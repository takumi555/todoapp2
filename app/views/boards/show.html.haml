%div
  = @board.title
%div
  = @board.content

%p All Tasks

- @tasks.each do |task|
  = link_to board_task_path(board_id: task.board_id, id: task.id) do
    - if task.image&.attached?
      = image_tag task.image
    .task_title
      = task.title
    .task_content
      = task.content 
    .task_deadline
      = task.deadline
.new_task_link
= link_to 'Task作成ページへ', new_board_task_path(@board)